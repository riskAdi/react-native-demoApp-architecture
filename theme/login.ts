import {normalize} from '../utils'

export const loginScreenTheme = {
	Button: {
		buttonStyle:{
			borderWidth:0 
		},
		titleStyle:{
			fontSize:normalize(16)
		},
		loginButton:{
			borderRadius:normalize(14),
			margin:normalize(10),
			height:normalize(40)
        }
    }

}
